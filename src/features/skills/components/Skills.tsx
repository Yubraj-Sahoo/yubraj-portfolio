import {TECHNICAL_EXPERTISE} from "../../../data";

export const Skills = () => {
    return (
        <section id={'skills'} className={'main_section'}>
            <p className="section__eyebrow">{TECHNICAL_EXPERTISE.eyebrow}</p>
        </section>
    );
};
